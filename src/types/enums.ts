export enum EQueryKeys {
  CURRENT_USER = 'curr-user',
  COMPANIES_LIST = 'companies list'
}

export enum ERole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  SUPER = 'SUPER-ADMIN'
}

export enum EOrder {
  ASC = 'ASC',
  DESC = 'DESC'
}
