export enum EAxiosPaths {
  LOGIN = '/auth/login',
  SIGNUP = '/auth/signup',
  RESET_PASSWORD = '/auth/reset-password',
  REFRESH_TOKEN = '/auth/refresh-token',
  USERS = '/users',
  COMPANIES = '/companies',
  CURRENT_USER = '/users/me'
}
